# 🔥 Firestore - База Клиентов

## Настройка

База данных клиентов автоматически создана в Firestore (коллекция `clients`). Права доступа используют те же настройки, что и сотрудники.

## Структура

```typescript
interface Client {
  id: string;              // CLI-XXXXXXXX
  name: string;            // Имя / Название
  type: 'individual' | 'legal';
  phone: string;
  email?: string;
  address?: string;
  
  // Юридические лица
  companyName?: string;
  inn?: string;
  mfo?: string;
  bankAccount?: string;
  bankName?: string;
  addressLegal?: string;

  // Финансы
  creditLimit: number;
  totalPurchases: number;
  totalDebt: number;       // Долг
  notes?: string;
  
  _version: number;
  updatedAt: string;
}
```

## Как использовать

Модуль "Клиенты" (CRM) теперь работает напрямую с Firebase.

### Возможности:
1. **Real-time обновления**: изменения видны мгновенно всем пользователям.
2. **Offline режим**: можно работать без интернета, данные синхронизируются при подключении.
3. **Безопасное хранение**: данные в облаке Google.

---

Если нужно перенести старых клиентов, свяжитесь с поддержкой (или используйте функцию `migrateFromSheets` в консоли).
